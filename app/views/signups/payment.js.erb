$('#signup-form').html("<%= escape_javascript render 'proceeding' %>");
// $('#signup')[0].style.height = '100vh';
$('button.stripe-button-el').click();
$('#payment-dialog').click(function () {
  $('button.stripe-button-el').click()
});
